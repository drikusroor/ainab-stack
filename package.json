{
  "name": "ainab-stack",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "ainab-stack": "bin/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "clean": "rm -rf dist",
    "build": "bun build ./bin/cli.ts --outfile=dist/ainab-stack --target=bun"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^14.0.2",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/prompts": "^2.4.9",
    "bun-types": "^1.3.5"
  }
}