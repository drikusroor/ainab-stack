{
  "name": "ainab-stack",
  "version": "0.0.4",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "ainab-stack": "bin/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "clean": "rm -rf dist",
    "build": "bun build ./bin/cli.ts --outfile=dist/ainab-stack --target=bun"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.0.3",
    "bun-types": "^1.3.5"
  }
}
